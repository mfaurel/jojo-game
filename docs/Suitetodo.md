En cours : 
- On the memory counting world, add a word when it's good "Bravo" or if, fail "Continue" with a glow so we know if we have won or not
- On the memory counting world, remove retour on top left and add Menu on top right. Clicking on menu, add a confirmation pop in to ask if we are sure to go back to menu
- On the memory world, first level of each row is unlocked by default
- On the memory world, the stars who fall when finishing the game are cropped on the top. Fix it by getting all the emoji visible. Also, fix it on all the winnning moment.
- On the math world, add a confirmation pop in to ask if we are sure to go back to menu
- On the spelling world, when failing three times, all the letters  that need to be input in green are colored instead of orange. Keep the colored letterrs if failing again
- On my collection, on "Mathématiques" tab, imagine two other items for left and right arm that will be implemented in the game and that are cute (candy cane for example and another one)
- On my collection, on "Mathématiques" tab, remove the two emoji on the left of "Bras"
- On my collection, on "Memory" tab, increase the size of the back of the cards
- On my collection, on "Memory" tab, color the string "Arc-en-ciel" in rainbow color


Corriger les bugs
- On my collection, I want the 31th star to be on top right of the screen glowing. Only by clicking it will get the 31th star on the main screen
- On the spelling game, fix menu on top which is bad centered, same for stars.. We don't need the name of the level on top.
- On the memory counting world, polish the level cards with emoji
- On all worlds, render the background to be on left and on right and extends when resizing the browser window.
- On my collection, on "Orthographe", "Mathematiques" and "Memory" tab, the word "Equiper" is too much low and touch the box. Same when you select it and it's written "Equipé". Put it on top of the cards. It's clearer


- On my collection, TODO FIX BUGS

Mettre à jour les sprites
- Put all the sprites of the words, monsters or cards of the different worlds in an html page in this repository so I can see in one glow all the used assets 
Ajouter du son
Ajouter une cinématique de fin quand on a toutes les étoiles
Ajouter une intro avec un logo "Deusgames"
Use default locale of browser ou ordi/telephone
Ajoute des achievements
Mode de jeu infini pour les mathématiques
Ajoute de l'allemand
Ajoute du chinois 
Render the dungeon crawler nicer - On the math world, I want you to propose some adjustment so the children thinks we are moving in a dungeon. Add some proper movement and make it better

Rajouter des skins pour le bundle premium (fille / garçon)
Create a google appstore application
Connection with Google 
Advertisement / Money
Connection with Facebook

Create a paid iOS application (or ads ?)